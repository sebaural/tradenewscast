# LiveSquawk feed URL (optional override)
LIVE_SQUAWK_URL=https://www.livesquawk.com/partner_messenger/embedded_content.php?partner_name=Infront2&

# Client poll interval in milliseconds (optional, default 30000)
# POLL_INTERVAL_MS=30000

# Max API requests per IP per minute (optional, default 30)
RATE_LIMIT_RPM=30
